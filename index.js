/* You might want to check first if the file exists and stuff but this is an example. */
const program = require('commander');

module.exports = function() {
    return "ciao" /* use 3 spaces of indentation */
}